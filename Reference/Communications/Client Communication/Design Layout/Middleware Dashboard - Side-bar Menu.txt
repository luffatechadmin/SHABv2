The sidebar should follow a clean, minimal, and structured vertical layout, inspired by the attached reference, with a soft background and subtle visual hierarchy to separate sections and interactions. At the top of the sidebar, place the SHAB logo and system name, followed by a search input field to allow quick navigation across modules. The search bar should have a rounded style, light border (#E5E7EB), and soft placeholder text, maintaining consistency with modern SaaS interfaces.

Below the search, the navigation should be organized into grouped menu sections, starting with the main functional modules. Each menu item should be presented with a left-aligned icon and label, using a clean line icon style consistent across all items. The default state of menu items should have a neutral text color (#1F2937) and transparent background, while hover states should introduce a very light background tint to indicate interactivity.

The “Summary” section should appear as a primary entry point, with “Attendance Analysis” as its child. This can be implemented either as a default-expanded dropdown or a single-level item depending on future scalability. The “Attendance Spreadsheet” section should behave as a collapsible dropdown containing “Daily”, “Weekly”, and “Monthly” views. When expanded, submenu items should appear slightly indented with reduced font weight and smaller spacing, clearly indicating hierarchy without adding visual clutter.

Similarly, the “Staff Records” section should include “Employee List” and “Shift Pattern”, and the “Raw Data” section should include “Device Records” and “Database Records”. Each of these parent items should include a chevron indicator (› / ▼) that rotates upon expansion, providing a clear visual cue of interaction. Only one dropdown may be expanded at a time (accordion style) to maintain a clean layout, unless multi-expand is required.

The active menu item should be clearly highlighted using a soft teal background (#D6F2EE) with navy text (#163A70) and optionally a left accent bar in teal to reinforce the active state. Icons for active items can also shift to the primary navy color to improve visibility. Submenu active states should follow the same pattern but with slightly lighter emphasis.

At the bottom of the sidebar, include the “Settings” menu as a standalone item, visually separated by spacing or a subtle divider line to indicate it as a secondary or system-level function. Optionally, this area can also include user profile information or quick actions in the future, following the same layout pattern as the reference design.

The sidebar should support a collapsible mode, where it reduces to an icon-only vertical strip. In this state, icons remain centered and tooltips appear on hover to display menu names. The transition between expanded and collapsed states should be smooth (around 200–250ms), preserving usability while optimizing screen space.

Overall, the sidebar design should emphasize clarity, hierarchy, and ease of navigation, using consistent spacing, rounded elements, and subtle color cues. The implementation should be component-based and dynamic, allowing menu items and permissions (e.g., admin-only sections) to be controlled via configuration, ensuring scalability for future feature expansion.